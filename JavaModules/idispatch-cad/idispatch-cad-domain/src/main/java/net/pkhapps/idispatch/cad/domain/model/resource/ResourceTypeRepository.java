package net.pkhapps.idispatch.cad.domain.model.resource;

import net.pkhapps.idispatch.domain.support.Repository;

/**
 * Repository interface for {@link ResourceType}.
 */
public interface ResourceTypeRepository extends Repository<ResourceTypeId, ResourceType> {
}
